import { env } from "cloudflare:workers";
export async function POST(request:Request){try{const body=await request.json() as Record<string,string>;if(!body.company||!body.contactName||!body.email||!body.message)return Response.json({error:'Required fields missing'},{status:400});await (env.DB as D1Database).prepare("INSERT INTO partner_leads (company,contact_name,email,message,status) VALUES (?,?,?,?,?)").bind(body.company,body.contactName,body.email,`${body.phone||''}\n${body.message}`,'new').run();return Response.json({ok:true})}catch{return Response.json({error:'Unable to save enquiry'},{status:500})}}
