"use client";
import { useState, type FormEvent } from "react";
import { Logo } from "../../components";

export default function AdminLogin(){const [status,setStatus]=useState(""),[submitting,setSubmitting]=useState(false);async function login(event:FormEvent<HTMLFormElement>){event.preventDefault();setSubmitting(true);setStatus("Signing in…");const form=new FormData(event.currentTarget);const response=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({username:form.get("username"),password:form.get("password")})});if(response.ok){window.location.assign("/admin");return}const data=await response.json() as {error?:string};setStatus(data.error||"Sign-in failed.");setSubmitting(false)}return <main className="admin-login"><section className="admin-login-card"><Logo/><div className="admin-login-icon"><i className="fa-solid fa-user-shield"/></div><p className="eyebrow">Secure administration</p><h1>Admin Login</h1><p>Sign in to manage applications, donations, partners, and website operations.</p><form onSubmit={login}><label>Username<div className="admin-field"><i className="fa-solid fa-user"/><input name="username" autoComplete="username" required autoFocus/></div></label><label>Password<div className="admin-field"><i className="fa-solid fa-lock"/><input name="password" type="password" autoComplete="current-password" required/></div></label><button className="btn btn-gold" type="submit" disabled={submitting}><i className="fa-solid fa-right-to-bracket"/> {submitting?"Signing in…":"Sign In"}</button><p className="form-status" role="alert">{status}</p></form><a href="/" className="admin-back"><i className="fa-solid fa-arrow-left"/> Back to website</a></section></main>}
